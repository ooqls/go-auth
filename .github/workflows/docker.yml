name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Test the auth
      run: make test_auth

  build:
    needs: test
    runs-on: ubuntu-latest
    services:
      docker:
        image: docker:20.10-dind
        options: --privileged

    steps:
    - uses: actions/checkout@v4
    - name: Build the base
      run: make build_base
    - name: Build the auth
      run: make build_auth
    - name: Build the roles
      run: make build_roles
    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ vars.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}
    - name: push the base
      run: make push_base
    - name: push the auth
      run: make push_auth
    - name: push the roles
      run: make push_roles